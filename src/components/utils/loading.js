import React from "react";
const IsLoadingHtml = (props) => {
    const {loadertype} = props;
    return (
        <div className="loader">
        
        </div>
    )
}

export default React.memo(IsLoadingHtml);